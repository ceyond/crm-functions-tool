# Integrations

## Zoho Accounts

- used for OAuth token refresh

## Zoho CRM

- used for function push, pull, sync, automation linking, and remote execution

## Local scripts

- `scripts/cli/run-zoho.js` executes remote function calls
- `scripts/cli/validate-deluge-file.js` validates Deluge locally
- `scripts/test/run-e2e-suite.js` coordinates the integration sequence

